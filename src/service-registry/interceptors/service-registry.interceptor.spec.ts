import { ServiceRegistryInterceptor } from './service-registry.interceptor';

describe('ServiceRegistryInterceptor', () => {
    it('should be defined', () => {
        expect(new ServiceRegistryInterceptor()).toBeDefined();
    });
});
